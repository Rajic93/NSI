// Import Actions
import {  } from './TwitterActions';

// Initial State
const initialState = {};

const TwitterReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default TwitterReducer;
