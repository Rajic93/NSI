// Import Actions
import {  } from './FacebookActions';

// Initial State
const initialState = {};

const FacebookReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default FacebookReducer;
