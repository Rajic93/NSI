// Import Actions
import {  } from './UserManagementActions';

// Initial State
const initialState = {};

const UserManagementReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default UserManagementReducer;
